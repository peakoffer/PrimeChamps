# Legacy migrations

These files predate managed Supabase migration history and are preserved for
reference only. They are incomplete as a production schema replay and must not
be passed to `supabase db push`.

Managed history starts at the baseline marker in `../migrations/`.
